import React from 'react';

import { connect } from 'react-redux';

import './MemoryStat.scss';

function MemoryStat() {
  return (
    <div>
      <h1>AAAAAA</h1>
    </div>
  );
}

function mapStateToProps() {
  return {};
}

function mapDispatchToProps() {
  return {};
}

MemoryStat.propTypes = {};

export default connect(mapStateToProps, mapDispatchToProps)(MemoryStat);
